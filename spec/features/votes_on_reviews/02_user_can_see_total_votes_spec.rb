require 'rails_helper'

feature "User, on a dev tool post, can see the total votes on each review" do
  scenario 'User sees number of upvotes on each review' do
    # foo
  end
  scenario 'User sees number of downvotes on each review' do
    # foo
  end
  scenario 'User sees aggrogate of up/down votes on each review' do
    # foo
  end
end
