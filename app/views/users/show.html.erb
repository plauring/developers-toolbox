<%= image_tag(current_user.profile_picture) %>

<h1> <%= @user.name %>'s Profile </h1>

<div class="field row">
  <div class="medium-6 columns">
    <h6>Your info</h6>
<ul>
  <li><%= @user.name %></li>
  <li><%= @user.email %></li>
  <li>Member since: <%= @user.created_at %></li>

</ul>
</div>
</div>

<div class="row"><%= link_to 'Edit your profile', edit_user_registration_path, class: "button" %></div>
